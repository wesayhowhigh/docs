---
sidebar_position: 5
sidebar_label: Force Lowercase
---

# Force Lowercase

When enabled, the site will always redirect to lowercase if any uppercase characters are detected, for canonical urls.

## Setup

`JUMP_HTTP_FORCE_LOWERCASE=true|false`, default to false.

```dotenv title=".env"
JUMP_HTTP_FORCE_LOWERCASE=true
```
