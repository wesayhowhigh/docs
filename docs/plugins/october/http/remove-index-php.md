---
sidebar_position: 9
sidebar_label: Remove index.php
---

# Remove `index.php`

When enabled, if the index.php is in the URL it is removed and redirected.

## Setup

`JUMP_REMOVE_INDEX_PHP=true|false`, defaults to `true`.

```dotenv title=".env"
JUMP_REMOVE_INDEX_PHP=true
```
