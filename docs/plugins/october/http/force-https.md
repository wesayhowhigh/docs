---
sidebar_position: 4
sidebar_label: Force HTTPS
---

# Force HTTPS

When enabled, the site to always redirects from http to https.

## Setup

`JUMP_HTTP_FORCE_HTTPS=true|false`, defaults to `false`.

```dotenv title=".env"
JUMP_HTTP_FORCE_HTTPS=true
```
